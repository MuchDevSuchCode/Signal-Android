*.ai binary
**/src/screenshotTest*/reference/**/*.png filter=lfs diff=lfs merge=lfs -text

# Prebuilt APKs are large binaries; keep them out of the git object store.
*.apk filter=lfs diff=lfs merge=lfs -text
